<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/pastel_green">

    <!-- Die WebView füllt den kompletten Bildschirm aus -->
    <WebView
        android:id="@+id/webview_home"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Ein Fallback-Text, falls keine URL in den Settings eingetragen wurde -->
    <TextView
        android:id="@+id/tv_home_placeholder"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="Bitte trage eine URL in den Einstellungen ein."
        android:textSize="18sp"
        android:textColor="#333333"
        android:textAlignment="center"
        android:padding="32dp"
        android:visibility="gone"/>

</FrameLayout>