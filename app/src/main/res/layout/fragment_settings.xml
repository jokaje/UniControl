<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="24dp">

    <Button
        android:id="@+id/btn_open_network_settings"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:backgroundTint="#8CA8B3"
        android:padding="16dp"
        android:text="Netzwerk Einstellungen"
        android:textColor="#FFFFFF"
        android:textSize="18sp"
        android:textStyle="bold" />

    <Button
        android:id="@+id/btn_open_design_settings"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:backgroundTint="#8CA8B3"
        android:padding="16dp"
        android:text="Design &amp; Farben anpassen"
        android:textColor="#FFFFFF"
        android:textSize="18sp"
        android:textStyle="bold" />

    <!-- NEU: Der Back-Up Button -->
    <Button
        android:id="@+id/btn_open_backup_settings"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:backgroundTint="#8CA8B3"
        android:padding="16dp"
        android:text="Back-Up Einstellungen"
        android:textColor="#FFFFFF"
        android:textSize="18sp"
        android:textStyle="bold" />

</LinearLayout>